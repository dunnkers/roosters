{{#if isNested}}

  <table>
    <tbody>
      {{#each cell in nested}}
        <tr>
          <td>

            {{cell-display lessons=cell.content}}

          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

{{else}}
    {{sibling-lesson siblings=siblings.content}}
{{/if}}
