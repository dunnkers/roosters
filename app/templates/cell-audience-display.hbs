{{#if lesson.group}}
    <small class="pull-right">
      {{#link-to 'schedule' lesson.group.id}}{{lesson.group.id}}{{/link-to}}
    </small>
{{else}}
    {{#if lesson.cluster}}
        <small class="pull-right">
          {{lesson.cluster.id}}
        </small>
    {{/if}}
{{/if}}
