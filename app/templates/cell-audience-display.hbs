{{#if group}}
    <small class="pull-right">
      {{#link-to 'schedule' group.id}}{{group.id}}{{/link-to}}
    </small>
{{else}}
    {{#if cluster}}
        <small class="pull-right">
          {{cluster.id}}
        </small>
    {{/if}}
{{/if}}
