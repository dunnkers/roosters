<div class="col-xs-12 col-sm-9 col-sm-push-3">
    <h2>{{item.title}}</h2>

    {{render "schedule-display" item.schedule.lessons}}

    <small class="text-muted">{{item.schedule.updatedAt}}</small>
</div>
