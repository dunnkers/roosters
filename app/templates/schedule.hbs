<div class="col-xs-12 col-sm-9 col-sm-push-3">
    <h2>{{item.title}}</h2>

    {{render "schedule-display" item.schedule}}

    <small class="text-muted">
      Bijgewerkt &ndash; {{ago item.schedule.updatedAt}}
    </small>
</div>
