<div class="col-sm-4">
  <h3>{{id}}</h3>

  {{#if clusters}}
    <h4>Klassen</h4>
  {{/if}}

  {{audiences-list audiences=groups link="group"}}

  {{#if clusters}}
    <h4>Clusters</h4>

    {{audiences-list audiences=clusters link="cluster"}}
  {{/if}}
</div>

{{outlet}}
