{{#if cell.single}}

  {{! Ordinary- or sibling lesson }}
  {{#if cell.lesson.detailed}}

    {{! Table cell }}
    {{#link-to "lesson" cell.lesson tagName="td" class="cell" }}
      {{! Use both href and action for accessibility }}
      {{#link-to "lesson" cell.lesson}}

        {{#if cell.lesson.between}}
          <div>
            tussen
          </div>
        {{else}}
          <div>
            {{render "lesson-info" cell.combined}}
          </div>
        {{/if}}

      {{/link-to}}
    {{/link-to}}

  {{else}}
    <td></td>
  {{/if}}
{{else}}

  {{! Nested }}
  <td>
    <table>
      <tbody>
        {{#each cell in cell.nested}}
          <tr>
            {{render "cell" cell.content}}
          </tr>
        {{/each}}
      </tbody>
    </table>
  </td>
{{/if}}
